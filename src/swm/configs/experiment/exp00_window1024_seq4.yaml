# @package _global_
# Baseline (canonical v1 geometry). Inherits data/default window=1024, seq_len=4.
# Usage: python -m swm.<stage> +experiment=exp00_window1024_seq4 variant=B seed=0
exp_name: exp00_window1024_seq4
