# @package _global_
# GENERATED FROM experiments/configs/exp09_loss_exploit_ladder.yaml - DO NOT EDIT (edit the manifest and re-run swm.exp.gen_sweep)
# exp09_loss_exploit_ladder cell exp09_dpss_impulse_w0p025_fb0p01 (recipe hann0p3, geometry w256)
# packed: junction experiments/exp09_dpss_impulse_w0p025_fb0p01/packed -> experiments/exp01_window256_seq16/packed (the runner creates it).
# Running STAND-ALONE (e.g. a pilot under a throwaway exp_name) must add:
#   paths.packed_dir=C:/git_repo/Stellar-World-Model/experiments/exp01_window256_seq16/packed
# P12, pre-registered 2026-08-26 BEFORE wave 7 ran, scored on the 2x2 jointly (the fourth corner, w0p025 at fb=0.00, is the already-run wave-6 cell that collapsed). READ THE POWER NOTE ABOVE FIRST: at 6 seeds, 'no collapse observed' is NOT 'the collapse is fixed' (an unfixed cell at rate 1/12 shows a clean 6 about 59% of the time), and any arm clean at stage 1 gets seeds 6-11 before the claim is made. P12-A (free_bits is the operative lever): both fb=0.01 arms clean at stage 1 AND w0p03/fb=0.00 still collapses -> the fix is the KL floor, not the pressure. The cheap win is then w0p025 + fb0p01: the project's best 6-seed v1 probe, stabilised, with no weight increase and no severity cost at all. P12-B (pressure is the operative lever): w0p03/fb=0.00 is clean and the floor adds nothing detectable -> report free_bits as measured-unnecessary at w>=0.03 rather than untried, and prefer the simpler cell. P12-C (neither): a collapse survives in every new arm -> collapse is not a weight-or-floor problem, wave 5's P9 generalises fully, and the amount axis closes for good. This is the branch that needs the LEAST extra evidence, since a stage-1 collapse is conclusive on its own. P12-D (free_bits COSTS the probe): a floored arm is stable but regresses on G9-noregress where its unfloored twin does not -> the floor bought stability with representation quality. Report the trade; do NOT pick a winner on stability alone. Watch this: floor/KL is 0.81 at w=0.025 and 0.78 at w=0.03, close enough to bind on the weakest seeds. P12-E (the two levers are redundant, not additive): w0p03/fb0p01 is no cleaner than whichever single-lever arm works -> report the interaction as measured and prefer the one-knob cell. Added because wave 2 already caught exactly this shape once (P6c: DPSS and the kurtosis penalty were not independent). SEVERITY is a pre-registered SECONDARY, not the target: interpolation puts the w0p03 arms ~1.65 and the w0p025 arm ~1.55 (G17 PASS, G9 fail). If any lands above 2.0 it is recorded as a G17 failure and its probe is STILL read -- unlike G9-dose, G17 is not a VOID rule. Do NOT restate wave 6's encoder verdict on the strength of any of this; D17 is closed and this wave is Lane-M / journal.

exp_name: exp09_dpss_impulse_w0p025_fb0p01
data:
  seq_len: 16
  window: 256
  n_quiet: 10000
  val_frac: 0.15
  test_frac: 0.15
  max_absmax: 20.0
  num_workers: 0
  pack_workers: 8
  batch_size: 256
  limit: null
model:
  z_dim: 128
  in_ch: 1
  enc_channels:
  - 32
  - 64
  - 128
  - 256
  kernel_size: 5
  gru_hidden: 256
  gru_layers: 1
  dyn_mode: fwd_bwd
train:
  lambda_dyn: 60
  beta_target: 0.1
  beta_warmup_epochs: 10
  free_bits: 0.01
  lr: 0.0003
  lr_schedule: cosine
  lr_min: 3.0e-06
  max_epochs: 100
  patience: 10
  grad_clip: 1.0
  amp: true
  accum_steps: 1
  active_unit_kl_threshold: 0.01
  resume: true
  track_recon_aux_best: true
  track_recon_only_best: true
  select_include_dyn: false
  recon_aux:
    type: combined
    weight: 0.025
    psd_normalize: false
    psd_eps: 1.0e-08
    hf_weight: 1.0
    psd_window: dpss
    mask_frac: 0.0
    mask_span: 8
    psd_taper_nw: 4.0
    psd_taper_k: 7
    spectral_floor: null
    spectral_floor_mode: clamp
  impulse_penalty_weight: 0.1
  augment:
    hf_noise_sigma: 0.0
    hf_noise_min_uhz: 1000.0
  wandb:
    mode: online
    project: stellar-world-model
    entity: null
